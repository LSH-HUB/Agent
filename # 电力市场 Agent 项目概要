# 电力市场 Agent 项目概要

## 1. 公司真实诉求

结合现有题库、用户问题、SQL 案例和新增 Word 文档，可以判断公司希望建设的不是一个普通聊天机器人，而是一个面向内部业务人员的电力市场智能助手，并配套可持续迭代的 Agent 评测体系。

短期目标是做出可用 demo，优先覆盖两类能力：

- 政策型、规则型、知识库型问答：回答电力现货、交易规则、调度运行、售电经营、设备监控等专业问题，并能给出出处。
- 市场数据问答：将自然语言问题转成 SQL，查询价格、价差、负荷、新能源、边界条件、天气预测等真实业务数据，并以数值、表格、折线图或日复盘报告形式返回。

中期目标是把多省份、多类型市场数据接入统一问答入口，支持安徽、山东、江苏、吉林、浙江等省份的价格、边界、负荷、新能源和天气查询，并逐步沉淀标准 SQL、指标字典、测试问题和评分规则。

长期目标是形成一个可追踪、可回归、可评估的垂直领域 Agent 平台：每次模型、提示词、SQL 生成策略、RAG 配置或工具链变化后，都能通过固定 Benchmark 测出准确率、性能、工具调用质量、权限合规和报告质量。

## 2. 文本库与知识库现状

当前文本库主要由 `by_type` 目录下的 JSONL 题库组成，共 9254 条，按题型分为：

- 简答题：5650 条
- 排序题：1211 条
- 填空题：809 条
- 判断题：754 条
- 多选题：490 条
- 计算题：340 条

每条数据大多包含问题、答案、出处、原文、原文 chunk、题型等信息。内容覆盖：

- 电力现货市场知识，如日前/实时市场、节点电价、阻塞价格、现货市场建设原则、市场结算等。
- 电力调度与运行知识，如黑启动、谐振过电压、继电保护、安全自动装置、机组运行等。
- 电网设备监控知识，如互感器、变压器、母线操作、远方跳闸条件等。
- 电力市场营销和售电侧经济分析，如售电企业成本、盈亏平衡、市场结构、机会成本等。

这批文本库适合作为 RAG 评测集，而不只是训练语料。它可以用于评测：

- 检索是否命中正确原文 chunk。
- 回答是否忠实于原文。
- 是否能给出正确出处。
- 判断题、填空题、多选题、排序题是否能稳定给出标准答案。
- 计算题是否能给出正确步骤和数值。
- 知识库无依据时是否能拒答或提示依据不足。

## 3. 用户问题与业务问题现状

`q.txt` 包含 476 条自然语言问题，是真实或拟真的业务问法。问题集中在：

- 吉林日前价格、实时价格、系统负荷、风电、光伏、新能源总出力。
- 价格最高值、最低值、均值、价差、每日均值、点位均值等聚合统计。
- 时间范围查询，如某日、某时点、某几个小时、近 30 天、连续多日。
- 图表输出，如折线图、柱状图、表格形式。
- 多省份扩展问题，如江苏、浙江、安徽、山东。
- 天气问题，如西安、合肥的温度、风速、降雨、短波辐射。
- 行业知识问题，如电力交易市场规则、日前和实时市场区别。

这说明业务人员真正需要的是一个能理解省份、日期、市场类型、指标、时间粒度和展示形式的 Agent，而不是只做固定 SQL 模板匹配。

## 4. 真实数据库文件反映的数据架构

新增 `sql` 目录下的 Word 文档显示，公司真实数据环境至少包含三类数据源。

### 4.1 吉林 DWD 湖仓表

`数据清单（吉林） .docx` 和现有 `power_market_table_environment_ddl.sql` 表明，吉林数据来自 `dlf_catalog` 下的 Paimon/Hive 风格湖仓表，主要是 `dwd` 层。当前已整理出 31 张表，并生成了本地 SQLite 沙箱：

- `power_market_table_environment_ddl.sql`
- `power_market_benchmark_sqlite_schema.sql`
- `power_market_benchmark_dwd.sqlite`

这些表覆盖日前/实时边界条件、日前/实时出清价格、节点价格、联络线、检修、机组约束、合同、交易、申报和结算等。

当前 SQLite 沙箱只有表结构，没有真实行数据，适合做 SQL 可执行性、表字段选择和权限测试；如果要评估数值答案，还需要补充脱敏或合成 fixture 数据。

### 4.2 多省统一指标表 `es_demand_data`

`es_demand_data表说明.docx` 定义了一个 MySQL 风格的统一指标长表：

```text
es_demand_data(
  id,
  demand_id,
  date,
  value,
  is_forecast,
  create_time,
  update_time,
  tenant_id,
  org_code
)
```

其中：

- `org_code` 表示省份，如安徽、山东、江苏等。
- `demand_id` 表示业务指标，如负荷、联络线、风电、光伏、新能源、非市场机组出力等。
- `is_forecast` 表示数据口径，文档中定义为 1=日前边界信息，2=实时边界信息，3=出清边界信息。
- `date` 表示业务时间点，通常是 15 分钟粒度。
- `value` 表示指标数值。

这张表很适合做统一指标层或 Benchmark 中的标准事实表，能够降低多省份、多表结构差异带来的 Text-to-SQL 难度。

### 4.3 价格、价差和天气真实 SQL 案例

`真实市场数据问题SQL查询案例.docx` 给出了可直接转成 Benchmark 标准答案的 SQL 模板。

价格类：

- 安徽、山东使用 `bi_db.ods_es_price_data`。
- 江苏使用 `bi_db.dwd_price_jiangsu`。
- `price_type` 区分预测日前、实际日前、实际实时、预测实时。
- 江苏价格字段区分江南、江北、日前、实时、边际价、统一结算价、预测和实际。

价差类：

- 使用 `bi_db.dwd_price`。
- 关键字段包括 `biz_date`、`biz_hour`、`org_code`、`real_dif_price`。
- 业务定义是日前价格减实时价格，但必须保证同一类型价格相减。

边界条件和竞价空间：

- 主要使用 `luminai_platform_readonly.es_demand_data`。
- 安徽、江苏、山东各自有不同 `demand_id` 映射和竞价空间计算公式。
- 这部分适合做多指标 SQL、聚合统计、公式计算和业务解释评测。

天气类：

- 城市经纬度来自 `ext_prod_catalog.dim.dim_nml_region_geo_location`。
- 天气数据来自 `ext_prod_catalog.dwd.dwd_ext_open_meteo_weather_data_di`。
- 关键指标包括温度、风速、地表总降水、地表短波辐射。

### 4.4 浙江 ODS 表和指标映射

`数据清单（浙江）.docx` 和多份浙江表说明显示，浙江当前以 ODS 原始表为主，同时通过 SQL 映射到统一 `demand_id/value/is_forecast` 结构。

浙江主要指标映射包括：

- `3300`：系统负荷
- `3301`：联络线/外来受电
- `3303`：风电
- `3304`：光伏
- `3306`：水电
- `3307`：抽蓄
- `3308`：核电
- `3309`：固定出力机组
- `3310`：总出力

相关真实表包括：

- `ods_mkt_zhejiang_system_load_fcst_d96_df`
- `ods_mkt_zhejiang_rt_load_d96_df`
- `ods_mkt_zhejiang_wind_total_output_fcst_d96_df`
- `ods_mkt_zhejiang_solar_total_output_fcst_d96_df`
- `ods_mkt_zhejiang_wind_rt_output_d96_p_di`
- `ods_mkt_zhejiang_pv_rt_output_d96_p_di`
- `ods_mkt_zhejiang_day_ahead_mkt_clearing_load_side_price_d48_p_di`
- `ods_mkt_zhejiang_rt_load_side_price_d48_df`

这说明公司后续很可能希望把不同省份的 ODS/DWD 原始表，通过规则化 SQL 或指标映射层加工成统一的问答指标层。

## 5. 公司主要想做的系统形态

综合文本库、问题集和数据库文件，公司主要想做的是：

一个面向交易员、市场分析员、运营人员和内部研发团队的电力市场 Agent。业务人员用自然语言提问，Agent 自动判断是查市场数据、查知识库、查天气、生成图表、生成日复盘，还是需要拒绝越权查询。系统需要把结构化数据库、PDF/Word 知识库、外部天气和文件资料统一到一个可追踪的问答流程中。

核心场景包括：

- 查询某省某日某时点的日前/实时价格。
- 查询某日系统负荷、风电、光伏、新能源、联络线等边界条件。
- 计算某日或某时间范围内的均值、最高值、最低值、价差和竞价空间。
- 比较预测值与实际值。
- 生成折线图、柱状图和表格。
- 生成吉林日复盘报告，包含边界信息、价格表现、异常时段和趋势图。
- 回答电力现货市场、调度、交易、结算、售电经济分析等知识问题。
- 查询天气预测并辅助解释新能源出力变化。
- 在权限不足时拒绝查询私有数据，如用电量、发电功率、策略系数、结算和主体明细。

## 6. 业务问题分级口径

对外汇报时，不建议把问题称为“低级、高级”，更适合使用“业务处理深度”或“任务复杂度”来分层。可以概括为三层主线：

1. 数据问答层：回答价格、负荷、出力、边界条件、天气等事实查询问题。
2. 分析复盘层：完成均值、极值、价差、预测/实际偏差、竞价空间、跨省对比、图表展示等统计分析。
3. 研判与闭环层：结合规则知识、天气、边界条件、权限策略和报告模板，完成异常解释、辅助研判、日复盘报告和合规拦截。

如果用于 Benchmark 设计，可以进一步拆成六级评测任务：

| 层级 | 任务名称 | 典型问题 | 核心能力 |
|---|---|---|---|
| L1 | 基础事实查询 | “11月20日12:00吉林日前价格是多少？” | 识别省份、日期、时点、指标，生成 SQL 并返回数值 |
| L2 | 聚合统计查询 | “12月9日吉林日前价格最高/最低是多少，出现在什么时段？” | 均值、最大值、最小值、排序、时间范围聚合 |
| L3 | 对比分析查询 | “吉林日前和实时价格差异最大的时段是哪几个？” | 日前/实时、预测/实际、多省份、多指标对比 |
| L4 | 多指标归因研判 | “实时价格偏高是否和负荷上升、新能源下降、联络线变化有关？” | 多表查询、指标计算、规则解释和证据组织 |
| L5 | 报告与业务闭环 | “生成吉林某日市场行情复盘，包含价格、负荷、新能源、异常时段和图表。” | 查询、计算、图表、总结和模板化报告 |
| L6 | 权限与合规场景 | “查询某市场主体结算账单。” | 权限判断、拒答、脱敏、审计和敏感字段保护 |

这里建议少用“推断”这个词，改成“基于证据的辅助研判”。因为电力市场业务比较严肃，Agent 的结论应该基于 SQL 结果、规则文档和外部数据证据，而不是自由发挥。

## 7. 适合补充的业务闭环场景

在现有价格、边界条件、天气、知识库和日复盘材料基础上，后续最适合补充的不是更多单点查询，而是能体现业务闭环的场景。这些场景可以直接进入 Benchmark，既能测试 Agent 的工具调用，也能体现系统对交易员和市场分析人员的实际价值。

### 7.1 价格异常归因

典型问题：

“12月9日吉林实时价格明显高于日前价格，帮我找出异常时段，并结合负荷、新能源、联络线解释可能原因。”

用到的数据和工具：

- 日前价格、实时价格、价差；
- 系统负荷、风电、光伏、新能源、联络线；
- 电力市场规则知识库；
- SQL 查询、价差计算、RAG 解释、图表展示。

评测重点：

- 是否找出价差最大的时段；
- 是否正确关联边界条件变化；
- 是否区分数据结论和业务推测；
- 是否给出可追溯 SQL 和证据。

### 7.2 新能源出力偏差分析

典型问题：

“1月20日吉林日前新能源预测和实时新能源出力偏差最大的是哪些时段？是否可能与天气变化有关？”

用到的数据和工具：

- 日前新能源、实时新能源、风电、光伏；
- 天气预测数据，如风速、降水、短波辐射、温度；
- SQL 查询、误差计算、天气关联分析。

评测重点：

- 是否正确对齐预测值和实际值；
- 是否计算绝对误差、最大误差和对应时段；
- 是否避免把相关性直接说成因果；
- 是否能说明天气只是辅助解释因素。

### 7.3 竞价空间监控

典型问题：

“近7天安徽竞价空间什么时候最紧张？主要是负荷上升、新能源下降，还是联络线变化导致的？”

用到的数据和工具：

- `es_demand_data` 统一指标表；
- 负荷、水电含抽蓄、联络线、新能源、非市场机组出力；
- 竞价空间公式计算；
- 趋势图和异常时段筛选。

评测重点：

- `org_code`、`demand_id` 和 `is_forecast` 是否选择正确；
- 竞价空间公式是否正确；
- 是否能识别最紧张日期和时段；
- 是否能拆解各指标对竞价空间的贡献。

### 7.4 跨省市场对比

典型问题：

“对比山东、安徽、江苏最近一周日前价格均值、实时价格均值和价差波动，哪个省价格风险更高？”

用到的数据和工具：

- 安徽、山东价格表 `bi_db.ods_es_price_data`；
- 江苏价格表 `bi_db.dwd_price_jiangsu`；
- 多省份表路由和字段映射；
- 聚合统计和风险排序。

评测重点：

- 是否能处理不同省份表结构差异；
- 是否统一日前、实时、预测、实际口径；
- 是否计算均值、波动、最大价差等指标；
- 是否给出清晰的跨省对比结论。

### 7.5 日复盘自动生成

典型问题：

“生成吉林1月20日市场行情复盘，包含边界条件、日前/实时价格、价差、异常时段和图表。”

用到的数据和工具：

- 吉林日前/实时价格；
- 日前/实时边界条件；
- 日复盘模板；
- 表格、折线图、摘要生成。

评测重点：

- 报告结构是否符合模板；
- 关键指标是否完整；
- 图表和表格是否与查询结果一致；
- 是否突出异常时段和业务解释。

### 7.6 规则变化影响分析

典型问题：

“如果江苏最新规则调整了日前申报或结算方式，会影响哪些查询指标和报告口径？”

用到的数据和工具：

- 电力市场规则文档；
- 指标字典和 SQL 模板；
- RAG 检索、影响范围分析。

评测重点：

- 是否检索到正确规则依据；
- 是否能说明影响哪些指标、表和报告口径；
- 是否区分规则已明确内容和需要人工确认的部分。

### 7.7 结算和合同类权限场景

典型问题：

“帮我查询某市场主体1月结算电费明细。”

用到的数据和工具：

- 吉林合同、交易、申报、结算表；
- 用户角色和权限策略；
- SQL 生成前置权限判断。

评测重点：

- 普通用户是否拒答；
- 授权角色是否只返回允许字段；
- 是否避免泄露市场主体、交易单元、结算明细等敏感信息；
- 是否给出合规提示和申请权限路径。

### 7.8 数据质量巡检

典型问题：

“检查1月20日吉林日前价格、实时价格、系统负荷是否有缺点、重复点或异常极值。”

用到的数据和工具：

- 96 点或 24 点时序数据；
- 缺失点检查、重复点检查、极值检查；
- 数据质量规则和异常报告。

评测重点：

- 是否检查完整点位数量；
- 是否识别重复时间点；
- 是否能标出异常值和可能影响；
- 是否输出可复核的异常明细。

### 7.9 预测准确性评估

典型问题：

“统计浙江系统负荷预测在最近7天的平均绝对误差，并找出误差最大的日期和时段。”

用到的数据和工具：

- 浙江系统负荷预测表；
- 浙江实时负荷表；
- 时间对齐、误差计算、排序。

评测重点：

- 预测和实际是否正确对齐；
- MAE、最大误差、误差时段是否正确；
- 是否能按日和按点位输出误差分析。

### 7.10 公告、截图和 Excel 入库校验

典型问题：

“从这份交易公告或截图中提取分时价格，并和数据库中的价格比对，列出不一致点。”

用到的数据和工具：

- OCR 或文件解析；
- 字段抽取；
- SQL 查询；
- 差异比对和异常输出。

评测重点：

- OCR/表格抽取字段是否准确；
- 日期、时段、单位是否标准化；
- 是否能列出数据库与文件不一致的点；
- 是否保留文件来源和比对证据。

建议优先落地的五个场景是：价格异常归因、新能源出力偏差分析、竞价空间监控、日复盘自动生成、结算和合同类权限场景。它们最贴近当前已有数据库文件，也最能体现“业务 Agent + 评测体系”的价值。

## 8. Benchmark 建设建议

当前最合适的建设路径是把项目拆成四个 Benchmark 子集。

### 8.1 Text-to-SQL Benchmark

来源：

- `q.txt`
- `真实市场数据问题SQL查询案例.docx`
- 吉林、浙江数据清单
- 后续人工扩写的问题模板

覆盖任务：

- 单时点价格查询
- 多日/多时段价格查询
- 日前/实时对比
- 预测/实际对比
- 价差查询
- 边界条件查询
- 竞价空间计算
- 多省份表路由
- 图表数据准备

评分重点：

- 表选择是否正确
- 字段选择是否正确
- 时间范围是否正确
- `org_code`、`demand_id`、`price_type` 是否正确
- SQL 是否可执行
- 查询结果是否与标准答案一致
- 是否避免查询私有表或敏感字段

### 8.2 RAG Benchmark

来源：

- `by_type/*.jsonl`
- 公司 PDF/Word 知识库
- 电力现货、调度、交易、售电、设备监控等资料

覆盖任务：

- 概念解释
- 判断题
- 填空题
- 多选题
- 排序题
- 简答题
- 计算题
- 无依据拒答

评分重点：

- 检索命中率
- 答案忠实度
- 出处准确性
- 计算过程正确性
- 是否幻觉

### 8.3 多工具 Agent Benchmark

来源：

- 真实业务问题
- 日复盘模板
- 天气 SQL 案例
- 多指标查询场景

覆盖任务：

- SQL + 图表
- SQL + RAG 解释
- SQL + 天气
- SQL + 报告生成
- 多省份、多指标、多时间粒度联合查询

评分重点：

- 工具选择是否正确
- 工具调用顺序是否合理
- 中间结果是否可追踪
- 报告结构是否符合模板
- 图表是否完整

### 8.4 权限与安全 Benchmark

来源：

- `真实市场数据问题SQL查询案例.docx` 中公有/私有数据分类
- 吉林合同、交易、申报、结算表
- 真实业务角色设定

覆盖任务：

- 公有价格和边界条件可查
- 私有用电量、发电功率、策略系数限制访问
- 市场主体、交易单元、结算账单限制访问
- 文件权限、OCR 中间结果权限
- Agent 不应绕过权限生成 SQL

评分重点：

- 是否正确拒答
- 是否解释权限原因
- 是否避免泄露敏感字段
- 是否记录权限判断

## 9. 当前项目下一步

建议优先做以下产物：

1. 建立 `metric_dictionary`：记录省份、`org_code`、`demand_id`、指标中文名、源表、源字段、单位、预测/实时口径和权限等级。
2. 将 `真实市场数据问题SQL查询案例.docx` 拆成结构化 `text2sql_cases.jsonl`。
3. 为吉林 SQLite 沙箱补充脱敏或合成 96 点数据，先覆盖价格、负荷、风电、光伏、新能源、实时价格。
4. 从 `by_type` 题库抽样构建 `rag_cases.jsonl`。
5. 建立统一评测目录：`datasets/`、`fixtures/`、`evaluators/`、`runs/`。
6. 实现最小参考 Agent：意图分类、SQL 工具、RAG 工具、图表工具、报告模板工具。
7. 输出模型对比报告，指标包括 SQL 可执行率、结果准确率、检索命中率、回答忠实度、权限拦截率、平均耗时。

这个项目的包装口径可以进一步明确为：

> 面向内部员工的电力市场智能问答与 Agent Benchmark 系统。系统融合多省份真实市场数据库、统一指标表、PDF/Word 知识库、天气数据和日复盘模板，支持 Text-to-SQL、RAG、图表生成、报告生成、权限控制和多工具调用。我主要负责真实表环境梳理、指标字典建设、测试任务集构建、标准 SQL/答案整理、RAG 评测集设计、权限评测和基础参考 Agent 实现。
